/** Props for full-screen loading overlay component */
export interface AppLoadingOverlayProps {
  /** Controls whether overlay is visible */
  visible: boolean;

  /** Optional loading message displayed under/near spinner */
  message?: string;
}
